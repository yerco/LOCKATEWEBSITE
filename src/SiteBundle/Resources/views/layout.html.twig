{% trans_default_domain 'SiteBundle' %}
<!DOCTYPE html>
<html>
    <head>
    {%- block head -%}
        <meta charset="utf-8">
        <title>Lock-8 Sensing</title>
        <meta name="description" content="Lock-8 IoT - Sensors" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <!--stylesheets / link tags loaded here-->
        <link rel="stylesheet" href="{{- asset('bootstrap/css/bootstrap.min.css') -}}" />
        <link rel="stylesheet" href="{{- asset('bootstrap/css/font-awesome.min.css') -}}" />
        <link rel="stylesheet" href="{{- asset('css/layout.css') -}}" />
        {#<link rel="shortcut icon" href="{{ url_for('static', filename='images/favicon.ico') }}">#}

        {%- block nestedhead -%}
        {%- endblock -%}
    {%- endblock -%}
    </head>
    <body>
        {%- block header -%}
            <nav class="navbar-custom navbar-fixed-top navbar-inverse">
                <div class="container-fluid">
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        <a class="navbar-brand" href="#">Lock-8 Sensing</a>
                    </div>
                    <div id="navbar" class="collapse navbar-collapse">
                        <ul class="nav navbar-nav navbar-right">
                            <li><a href="">{% trans %}Timeline{% endtrans %}</a></li>
                            <li><a href="">{% trans %}Indicators{% endtrans %}</a></li>
                            <li><a href="">{% trans %}Projects{% endtrans %}</a></li>
                            <li><a href="">{% trans %}Smart-Nodes{% endtrans %}</a></li>
                            <li><a href="">{% trans %}Profile{% endtrans %}</a></li>
                            <li>
                                {%- if is_granted('ROLE_USER') -%}
                                    <a href="{{ path('site_bundle_security_logout') }}">Logout</a>
                                {%- else -%}
                                    Not logged and here talk to developer please
                                {%- endif -%}
                            </li>
                        </ul>
                    </div>{# /.nav-collapse #}
                </div> {# /.containe r#}
            </nav>{# /.navbar #}
        {%- endblock -%}

        {%- block body -%}

        {%- endblock -%}

        <hr>
        {%- block footer -%}
            <footer>
                <p>&copy; 2013 - 2018 Lock8/Lockate - Sensor Networks & Data Analytics</p>
            </footer>
        {%- endblock -%}

        <!--scripts loaded here-->
        <script src="{{ asset('js/jquery.min.js') }}"></script>
        <script src="{{ asset('js/bootstrap.min.js') }}"></script>
        <script>
            {# Note: multiple $(document).ready(...) run on first Called first run basis #}
            $(document).ready(function () {
                $('[data-toggle="offcanvas"]').click(function () {
                    $('.row-offcanvas').toggleClass('active')
                });
            });
        </script>
    </body>
</html>
